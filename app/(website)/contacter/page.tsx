import { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Nous contacter | Rivières du monde',
	description: '...',
};

export default function Page() {
	return <h1>Contacter</h1>
}